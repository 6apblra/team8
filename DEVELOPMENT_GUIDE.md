# 🚀 Оптимизированный Гайд Разработки

## Быстрый Старт (30 секунд)

### Вариант 1: Docker (Рекомендуется для новичков)
```bash
./scripts/dev-start.sh          # Все сразу: DB + Backend + Frontend
```

### Вариант 2: Локально (Для опытных)
```bash
./scripts/dev-backend.sh &      # Backend в отдельном процессе
npm run expo:dev                # Frontend в текущем терминале
```

### Вариант 3: Полностью локально (Максимальный контроль)
```bash
./scripts/local-backend.sh      # Backend с Python venv
npm run expo:dev                # Frontend
```

---

## 🛠️ Ежедневный Workflow

### 1️⃣ Развернуть проект
```bash
# Первый раз:
npm install && docker-compose up -d

# Следующие разы (если уже запущено):
docker-compose ps              # Проверить статус сервисов
npm run expo:dev               # Запустить frontend
```

### 2️⃣ Разработка компонентов
```bash
# Изменения автоматически перезагружаются (hot reload)
# Просто отредактируй файл и сохрани
```

### 3️⃣ Изменения в базе данных
```bash
# 1. Отредактировать models.py (backend)
# 2. Создать миграцию
alembic revision --autogenerate -m "описание"

# 3. Применить миграцию
alembic upgrade head
```

### 4️⃣ Добавить новый API эндпоинт
```bash
# 1. Создать роутер в backend/app/routers/new_feature.py
# 2. Добавить в main.py:
#    app.include_router(routers.new_feature.router)
# 3. Использовать в frontend через api-client.ts
```

### 5️⃣ Запустить тесты
```bash
npm run lint                   # Проверить синтаксис
npm run check:types           # Проверить типы
npm run test:backend          # Тесты backend (будут добавлены)
```

### 6️⃣ Коммитить изменения
```bash
git add .
git commit -m "feature: описание"
```

---

## 📊 Чек-лист перед Push

- [ ] `npm run check:types` - без ошибок
- [ ] `npm run lint` - без ошибок
- [ ] Приложение запускается: `npm run expo:dev`
- [ ] Логические тесты пройдены
- [ ] Миграции БД применены (если были изменения)
- [ ] `.env` переменные не закомичены

---

## 🐛 Отладка

### Backend ломается при старте?
```bash
# 1. Проверить логи
docker-compose logs backend

# 2. Пересоздать контейнеры
docker-compose down && docker-compose up -d

# 3. Сбросить БД и переинициализировать
docker volume rm teamup_postgres_data
docker-compose up -d
```

### Frontend не видит API?
```bash
# 1. Проверить EXPO_PUBLIC_API_URL в .env
cat .env | grep API_URL

# 2. Проверить доступ к API
curl http://192.168.0.112:8000/health

# 3. Очистить кэш Expo
rm -rf .expo
npx expo start --localhost
```

### WebSocket не подключается?
```bash
# Проверить токен в localStorage
# Убедиться что токен правильно передаётся в ?token=...
# Проверить logs в DevTools на мобильном
```

---

## 📁 Структура проекта

```
team8/
├── backend/              # FastAPI приложение
│   ├── app/
│   │   ├── main.py      # Основное приложение
│   │   ├── models.py    # Модели БД (изменяй здесь)
│   │   ├── routers/     # API эндпоинты (добавляй здесь)
│   │   └── websocket.py # Real-time коммуникация
│   ├── alembic/         # Миграции БД
│   └── seed.py          # Тестовые данные
│
├── client/               # React Native приложение
│   ├── components/       # UI компоненты
│   ├── screens/          # Экраны приложения
│   ├── lib/
│   │   ├── api-client.ts # HTTP клиент к API
│   │   └── auth-context.tsx # Управление авторизацией
│   └── App.tsx
│
├── server/               # Node.js backend (альтернатива FastAPI)
│   ├── index.ts         # Express приложение
│   ├── routes.ts        # Эндпоинты
│   └── storage.ts       # БД запросы
│
└── scripts/              # Утилиты разработки
    ├── dev-start.sh     # Полный старт (Docker)
    ├── dev-backend.sh   # Только backend (Docker)
    └── local-backend.sh # Backend локально
```

---

## 🔧 Полезные команды

```bash
# Разработка
npm run expo:dev                # Start Expo dev server
npm run server:dev              # Start Node.js backend
npm run lint                    # Lint code
npm run format                  # Format code
npm run check:types             # Type checking

# Docker
docker-compose up -d            # Start all services
docker-compose down             # Stop all services
docker-compose logs -f backend  # Show backend logs
docker-compose ps               # Show service status

# Database
alembic revision --autogenerate # Create migration
alembic upgrade head            # Apply migrations
alembic downgrade base          # Rollback all migrations

# Testing
npm run test:api                # Test API (curl-based)
npm run test:profile            # Test profile endpoint
npm run test:swipes             # Test swipe logic
```

---

## 📝 Гайд по типичным задачам

### Добавить новый игровой режим
1. Добавить в `backend/app/models.py` новую Game запись
2. Обновить `backend/seed.py` для тестирования
3. Пересоздать контейнер: `docker-compose up -d --build`
4. Обновить `client/lib/game-data.ts` с новыми режимами

### Изменить профиль пользователя
1. Отредактировать `backend/app/models.py` (таблица profiles)
2. Создать миграцию: `alembic revision --autogenerate -m "Add new field"`
3. Применить: `alembic upgrade head`
4. Обновить `backend/app/schemas.py` (Pydantic модели)
5. Обновить `backend/app/routers/profile.py` (обработка запроса)
6. Обновить `client/` форму редактирования профиля

### Добавить новый API эндпоинт
1. Создать файл `backend/app/routers/feature.py`
2. Добавить в `backend/app/main.py`: `app.include_router(routers.feature.router)`
3. Использовать в `client/lib/api-client.ts`: добавить метод
4. Вызвать в компоненте через React Query

### Добавить валидацию
1. Обновить `backend/app/schemas.py` (Pydantic валидация)
2. Обновить `shared/validation.ts` (Zod валидация на клиенте)
3. Обновить форму в `client/screens/` с новой валидацией

---

## 🚢 Развертывание

### На staging сервере
```bash
git push origin feature-branch
# CI/CD автоматически протестирует

# После merge в main:
# 1. Сервер автоматически обновится
# 2. Миграции БД применятся
# 3. Приложение перезагрузится
```

### На production
```bash
# Убедиться что все тесты прошли в main
git tag v1.0.0
git push origin v1.0.0

# Запустить deployment скрипт (если настроен)
./scripts/deploy-prod.sh
```

---

## 📚 Документация

- [README.md](README.md) - Обзор проекта
- [QUICKSTART.md](QUICKSTART.md) - Очень быстрый старт
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - Полная структура
- [DEPLOYMENT.md](DEPLOYMENT.md) - Production развертывание

---

## 🤝 Помощь и поддержка

- Все ошибки начинаются с `docker-compose logs -f`
- Перед push проверь `npm run check:types`
- Для новых фич используй существующие компоненты как шаблон
- Задавай вопросы в Issues если что-то непонятно
