````markdown
# 🚀 Быстрый старт TeamUp MVP

## Запуск за 3 шага

### 1. Запустите Docker Compose

```bash
docker-compose up -d
```

Это запустит:
- ✅ PostgreSQL базу данных
- ✅ FastAPI backend на http://localhost:8000
- ✅ Автоматически создаст таблицы
- ✅ Заполнит тестовыми данными

### 2. Проверьте API

Откройте в браузере: http://localhost:8000/docs

Или проверьте через curl:
```bash
curl http://localhost:8000/health
# Должен вернуть: {"status":"ok"}
```

### 3. Запустите мобильное приложение

```bash
# Установите зависимости (если еще не установлены)
npm install

# Запустите Expo
npm run expo:dev
# или
npx expo start
```

## Тестовые аккаунты

После запуска seed скрипта доступны 5 тестовых пользователей:

| Email | Password | Описание |
|-------|----------|----------|
| player1@test.com | password123 | ProGamer (Valorant, Immortal) |
| player2@test.com | password123 | CasualPlayer (Valorant, CS2) |
| player3@test.com | password123 | FlexGamer (Dota2, LoL) |
| player4@test.com | password123 | RusGamer (CS2, Global Elite) |
| player5@test.com | password123 | FortnitePro (Fortnite) |

## Настройка API URL для мобильного приложения

Отредактируйте `client/lib/api-client.ts`:

```typescript
// Для iOS симулятора:
const API_BASE_URL = "http://localhost:8000";

// Для Android эмулятора:
const API_BASE_URL = "http://10.0.2.2:8000";

// Для физического устройства (замените на IP вашего компьютера):
const API_BASE_URL = "http://192.168.1.100:8000";
```

Или установите переменную окружения:
```bash
export EXPO_PUBLIC_API_URL=http://localhost:8000
```

## Что дальше?

1. **Зарегистрируйтесь** или войдите с тестовым аккаунтом
2. **Создайте профиль** - заполните nickname, выберите игры, установите доступность
3. **Свайпайте** - открывайте ленту и свайпайте вправо (like) или влево (pass)
4. **Чатите** - при взаимном лайке откроется матч, можно начать общаться

## Полезные команды

```bash
# Остановить все сервисы
docker-compose down

# Пересоздать базу данных (удалит все данные!)
docker-compose down -v
docker-compose up -d

# Просмотр логов
docker-compose logs -f backend
docker-compose logs -f db

# Запуск миграций вручную
cd backend
alembic upgrade head

# Заполнение seed данных вручную
cd backend
python seed.py
```

## Troubleshooting

**Проблема:** База данных не подключается
```bash
# Проверьте, что PostgreSQL запущен
docker ps

# Перезапустите сервисы
docker-compose restart
```

**Проблема:** Mobile app не подключается к API
- Убедитесь, что используете правильный IP адрес
- Проверьте, что backend доступен: `curl http://localhost:8000/health`
- Для физического устройства убедитесь, что телефон и компьютер в одной сети

**Проблема:** WebSocket не работает
- Проверьте, что токен передается правильно
- Убедитесь, что backend запущен и доступен
- Проверьте логи: `docker-compose logs backend`

## API Endpoints

- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход
- `GET /auth/me` - Текущий профиль
- `PUT /me/profile` - Создать/обновить профиль
- `GET /feed?game=Valorant` - Лента кандидатов
- `POST /swipe` - Свайпнуть пользователя
- `GET /matches` - Список матчей
- `GET /matches/{id}/messages` - История сообщений
- `POST /matches/{id}/messages` - Отправить сообщение
- `WS /ws?token=...` - WebSocket для realtime чата

Полная документация API: http://localhost:8000/docs

См. [DEPLOYMENT.md](DEPLOYMENT.md) для сведений о переменных окружения и рекомендаций по продакшн-развёртыванию.


````
